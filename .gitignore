# ── Large data & generated artifacts (not for repo) ──────────────────────────
# raw + processed CHARIS/MIMIC arrays (large, credentialed)
data/
# trained model binaries + per-fold caches
models/
# generated result JSONs and plots
results/

# ── Raw human-subject recordings (privacy — keep OUT of the repo) ─────────────
hw-tests/
icp_session_*.csv

# ── Manuscript (kept local until the 100-subject version is finalized) ───────
paper/

# ── Logs ─────────────────────────────────────────────────────────────────────
logs/
*.log

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
.pytest_cache/

# ── Editor / tooling ─────────────────────────────────────────────────────────
.claude/
_dup/
prof_justification.tex

# ── Environment / secrets ────────────────────────────────────────────────────
.env
*.env.local

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Node ─────────────────────────────────────────────────────────────────────
node_modules/

# ── support/ holds moved experiments and artefacts (see support/MANIFEST.md); not part of the showcase ──
support/

# ── Generated presentation ───────────────────────────────────────────────────
*.pptx

# NOTE: assets/ IS tracked (README showcase figures). paper/ stays local for now.
